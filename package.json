{
  "name": "muhan-web-runner",
  "version": "0.3.0",
  "private": true,
  "description": "Browser-playable web gateway for the MUHAN legacy MUD server.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/server.js",
    "check": "node --check server/server.js && node --check web/app.js && node --check scripts/wait-for-tcp.js && node --check scripts/healthcheck.js && node --check scripts/smoke-gateway.js",
    "test": "node scripts/smoke-gateway.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
